name: terradart_example_network_services_mesh_quickstart
description: Network Services Mesh end-to-end example — global mesh plus HTTP/gRPC/TCP routes and an endpoint policy.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
