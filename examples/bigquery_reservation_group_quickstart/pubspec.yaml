name: terradart_example_bigquery_reservation_group_quickstart
description: BigQuery reservation group end-to-end example — empty slots container.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
