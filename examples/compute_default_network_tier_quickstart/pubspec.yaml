name: terradart_example_compute_default_network_tier_quickstart
description: Compute Engine project default network tier quickstart (STANDARD).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.0
  terradart_google: ^0.25.0

dev_dependencies:
  lints: ^6.0.0
