name: terradart_example_dataplex_quickstart
description: terradart example — Dataplex data product + IAM member.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.14.0
  terradart_google: ^0.14.0

dev_dependencies:
  lints: ^6.0.0
