name: terradart_example_ops_quickstart
description: Ops quickstart — Wave 2 + Wave 11 logging project ops (sink, bucket, log view, metric).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.19
  terradart_google: ^0.12.19

dev_dependencies:
  lints: ^6.0.0
