name: terradart_example_ops_quickstart
description: Phase 4.5 Wave 2 quickstart -- logging_project_sink routing audit logs to BigQuery with typed helpers.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.5
  terradart_google: ^0.12.5

dev_dependencies:
  lints: ^6.0.0
