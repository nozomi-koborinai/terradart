name: terradart_example_service_directory_quickstart
description: Service Directory end-to-end example -- a namespace, a service, an endpoint, and namespace/service IAM members, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.26.0
  terradart_google: ^0.26.0

dev_dependencies:
  lints: ^6.0.0
