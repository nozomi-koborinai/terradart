name: terradart_example_oracle_goldengate_quickstart
description: terradart example — Oracle GoldenGate deployment, connection, and assignment.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.22.0
  terradart_google: ^0.22.0

dev_dependencies:
  lints: ^6.0.0
