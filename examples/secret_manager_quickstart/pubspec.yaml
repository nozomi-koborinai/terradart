name: terradart_example_secret_manager_quickstart
description: terradart example -- Secret Manager with write-only secret_data_wo + IAM accessor grant.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.18.0
  terradart_google: ^0.18.0

dev_dependencies:
  lints: ^6.0.0
