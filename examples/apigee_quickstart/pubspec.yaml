name: terradart_example_apigee_quickstart
description: terradart example — Apigee data collector and GCS analytics datastore.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.16.0
  terradart_google: ^0.16.0

dev_dependencies:
  lints: ^6.0.0
