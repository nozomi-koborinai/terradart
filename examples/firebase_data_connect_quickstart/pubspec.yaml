name: terradart_example_firebase_data_connect_quickstart
description: Firebase Data Connect quickstart -- serverless GraphQL backend service.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.20
  terradart_google: ^0.12.20

dev_dependencies:
  lints: ^6.0.0
