name: terradart_example_firebase_app_check_quickstart
description: Firebase App Check quickstart -- attestation providers + service enforcement.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.18.0
  terradart_google: ^0.18.0

dev_dependencies:
  lints: ^6.0.0
