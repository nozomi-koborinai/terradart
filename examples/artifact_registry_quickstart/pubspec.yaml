name: terradart_example_artifact_registry_quickstart
description: Artifact Registry project config quickstart — platform logs for a location.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
