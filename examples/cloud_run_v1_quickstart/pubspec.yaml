name: terradart_example_cloud_run_v1_quickstart
description: Cloud Run v1 hello service quickstart (no min instances).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
