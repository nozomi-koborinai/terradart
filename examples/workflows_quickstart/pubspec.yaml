name: terradart_example_workflows_quickstart
description: Workflows end-to-end example -- a serverless orchestration workflow defined from inline YAML, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.27.0
  terradart_google: ^0.27.0

dev_dependencies:
  lints: ^6.0.0
