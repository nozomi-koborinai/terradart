name: terradart_example_notebooks_quickstart
description: Vertex AI Workbench environment quickstart — container image recipe (no VM).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.3
  terradart_google: ^0.25.3

dev_dependencies:
  lints: ^6.0.0
