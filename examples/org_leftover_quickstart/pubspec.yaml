name: terradart_example_org_leftover_quickstart
description: terradart example — apply-excluded org/BYOIP/Wasm leftovers.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.2
  terradart_google: ^0.25.2

dev_dependencies:
  lints: ^6.0.0
