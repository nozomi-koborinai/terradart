name: terradart_example_monitoring_quickstart
description: Phase 4.5 Wave 3 quickstart -- alert policy with typed condition_threshold + Aligner/Reducer enums + alert_strategy.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.5
  terradart_google: ^0.12.5

dev_dependencies:
  lints: ^6.0.0
