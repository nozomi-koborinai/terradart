name: terradart_example_monitoring_quickstart
description: Monitoring quickstart — Wave 3 + Wave 12 (channel, uptime, SLO, dashboard, alert).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
