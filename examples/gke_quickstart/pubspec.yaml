name: terradart_example_gke_quickstart
description: GKE quickstart — VPC, cluster, node pool, and GKE Hub fleet membership.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.15
  terradart_google: ^0.12.15

dev_dependencies:
  lints: ^6.0.0
