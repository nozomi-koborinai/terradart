name: terradart_example_storage_quickstart
description: Phase 4.5 Wave 1 quickstart -- GCS bucket + bucket_object with typed Dart enums and helper classes.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.17.0
  terradart_google: ^0.17.0

dev_dependencies:
  lints: ^6.0.0
