name: terradart_example_firebase_remote_config_quickstart
description: Firebase Remote Config quickstart -- parameters, conditions, and parameter groups.
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  terradart_core:
    path: ../../packages/terradart_core
  terradart_google:
    path: ../../packages/terradart_google

dev_dependencies:
  lints: ^6.0.0
