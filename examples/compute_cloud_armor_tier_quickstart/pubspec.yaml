name: terradart_example_compute_cloud_armor_tier_quickstart
description: Compute Engine project Cloud Armor tier quickstart (CA_STANDARD).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.3
  terradart_google: ^0.25.3

dev_dependencies:
  lints: ^6.0.0
