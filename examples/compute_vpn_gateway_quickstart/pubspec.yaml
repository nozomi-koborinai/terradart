name: terradart_example_compute_vpn_gateway_quickstart
description: Compute VPN gateway shells quickstart (no tunnels).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
