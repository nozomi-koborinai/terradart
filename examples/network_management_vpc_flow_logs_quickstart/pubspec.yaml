name: terradart_example_network_management_vpc_flow_logs_quickstart
description: Network Management VPC Flow Logs config on an empty VPC.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.0
  terradart_google: ^0.25.0

dev_dependencies:
  lints: ^6.0.0
