name: terradart_example_bigtable_quickstart
description: terradart example -- Cloud Bigtable instance, table, views, GC, IAM.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.0
  terradart_google: ^0.25.0

dev_dependencies:
  lints: ^6.0.0
