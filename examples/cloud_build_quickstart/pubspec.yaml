name: terradart_example_cloud_build_quickstart
description: Wave 7 Batch 1 quickstart -- Cloud Build CI/CD chain (cloudbuildv2 connection + repository + artifact registry + worker pool + trigger).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.26.0
  terradart_google: ^0.26.0

dev_dependencies:
  lints: ^6.0.0
