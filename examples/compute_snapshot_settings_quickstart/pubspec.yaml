name: terradart_example_compute_snapshot_settings_quickstart
description: Compute Engine project snapshot settings quickstart (LOCAL_REGION policy).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.2
  terradart_google: ^0.25.2

dev_dependencies:
  lints: ^6.0.0
