name: terradart_example_gemini_quickstart
description: Gemini for Google Cloud example -- project enablement, logging, and release-channel settings, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.27.0
  terradart_google: ^0.27.0

dev_dependencies:
  lints: ^6.0.0
