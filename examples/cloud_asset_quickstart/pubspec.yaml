name: terradart_example_cloud_asset_quickstart
description: Cloud Asset Inventory project feed to Pub/Sub.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.0
  terradart_google: ^0.25.0

dev_dependencies:
  lints: ^6.0.0
