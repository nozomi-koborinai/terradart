name: terradart_example_agentic_applications_quickstart
description: Gemini Enterprise analyst agent persona grounded on an in-stack BigQuery table.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.26.0
  terradart_google: ^0.26.0

dev_dependencies:
  lints: ^6.0.0
