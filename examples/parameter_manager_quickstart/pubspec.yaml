name: terradart_example_parameter_manager_quickstart
description: Parameter Manager end-to-end example -- a global and a regional application-config parameter, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.18.0
  terradart_google: ^0.18.0

dev_dependencies:
  lints: ^6.0.0
