name: terradart_example_vector_quickstart
description: Vector Search 2.0 collection quickstart (schema-only).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.27.0
  terradart_google: ^0.27.0

dev_dependencies:
  lints: ^6.0.0
