name: terradart_example_oracle_db_system_quickstart
description: terradart example — Oracle Base Database DB System on ODB networking.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.23.0
  terradart_google: ^0.23.0

dev_dependencies:
  lints: ^6.0.0
