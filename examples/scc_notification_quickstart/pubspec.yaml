name: terradart_example_scc_notification_quickstart
description: Security Command Center v2 project notification config end-to-end example.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
