name: terradart_example_ncc_hub_quickstart
description: Network Connectivity Center hub, group, VPC spoke, internal range, regional endpoint.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
