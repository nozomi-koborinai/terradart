name: terradart_example_aws_static_site_quickstart
description: A Flutter Web build hosted on a private S3 bucket behind CloudFront, served on a custom domain with an ACM certificate validated through Route 53.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.28.1
  terradart_aws: ^0.28.1

dev_dependencies:
  lints: ^6.0.0
