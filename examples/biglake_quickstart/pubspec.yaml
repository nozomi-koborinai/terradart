name: terradart_example_biglake_quickstart
description: BigLake Metastore example -- a Hive-compatible catalog, database, and table, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
