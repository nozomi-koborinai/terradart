name: terradart_example_aws_lambda_quickstart
description: Smallest terradart_aws example -- a Dart AOT binary on the Lambda provided.al2023 runtime behind a function URL, with its execution role and log group.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.28.1
  terradart_aws: ^0.28.1

dev_dependencies:
  lints: ^6.0.0
