name: terradart_example_clouddeploy_quickstart
description: Cloud Deploy end-to-end example -- a Cloud Run delivery target, a delivery pipeline, and a custom target type, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
