name: terradart_example_developer_connect_quickstart
description: Developer Connect GitHub account connector quickstart (no connection).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.24.0
  terradart_google: ^0.24.0

dev_dependencies:
  lints: ^6.0.0
