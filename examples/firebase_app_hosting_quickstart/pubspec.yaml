name: terradart_example_firebase_app_hosting_quickstart
description: Firebase App Hosting quickstart -- Cloud Run-backed backend with a custom domain.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.13
  terradart_google: ^0.12.13

dev_dependencies:
  lints: ^6.0.0
