name: terradart_example_firestore_quickstart
description: Cloud Firestore quickstart -- Native-mode database with point-in-time recovery and a composite index on a "messages" collection.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.13.0
  terradart_google: ^0.13.0

dev_dependencies:
  lints: ^6.0.0
