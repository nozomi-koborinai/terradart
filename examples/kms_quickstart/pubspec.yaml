name: terradart_example_kms_quickstart
description: Phase 4.5 Wave 0 quickstart -- KMS key ring.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.0
  terradart_google: ^0.12.0

dev_dependencies:
  lints: ^6.0.0
