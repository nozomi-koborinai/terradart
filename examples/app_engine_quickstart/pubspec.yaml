name: terradart_example_app_engine_quickstart
description: App Engine end-to-end example — application, standard/flex versions, firewall, dispatch, domain mapping, and traffic controls.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
