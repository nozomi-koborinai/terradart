name: terradart_example_config_deployment_quickstart
description: terradart example — Infrastructure Manager deployment from a Git blueprint.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.20.0
  terradart_google: ^0.20.0

dev_dependencies:
  lints: ^6.0.0
