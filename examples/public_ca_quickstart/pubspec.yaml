name: terradart_example_public_ca_quickstart
description: Public CA external account binding (EAB) key for ACME.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
