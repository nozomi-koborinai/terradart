name: terradart_example_project_iam_audit_config_quickstart
description: Project IAM audit logging (ADMIN_READ) plus a project deny policy.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.3
  terradart_google: ^0.25.3

dev_dependencies:
  lints: ^6.0.0
