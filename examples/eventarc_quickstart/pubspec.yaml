name: terradart_example_eventarc_quickstart
description: Eventarc quickstart — Waves 17 (channel, message bus, pipeline, trigger).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.17.1
  terradart_google: ^0.17.1

dev_dependencies:
  lints: ^6.0.0
