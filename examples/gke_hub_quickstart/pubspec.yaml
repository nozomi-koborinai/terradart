name: terradart_example_gke_hub_quickstart
description: GKE Hub fleet team-management example -- a fleet scope, namespace, and scope RBAC binding (no cluster required).
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
