name: terradart_example_pubsub_quickstart
description: Smallest terradart end-to-end example -- Pub/Sub topic + push subscription + IAM member, with typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  terradart_core:
    path: ../../packages/terradart_core
  terradart_google:
    path: ../../packages/terradart_google

dev_dependencies:
  lints: ^6.0.0
