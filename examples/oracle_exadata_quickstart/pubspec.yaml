name: terradart_example_oracle_exadata_quickstart
description: terradart example — Oracle Exadata, ExaDB, and Exascale on ODB networking.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.19.0
  terradart_google: ^0.19.0

dev_dependencies:
  lints: ^6.0.0
