name: terradart_example_compute_preview_feature_quickstart
description: Compute Engine preview feature quickstart.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.1
  terradart_google: ^0.25.1

dev_dependencies:
  lints: ^6.0.0
