name: terradart_example_tags_quickstart
description: Resource Manager Tags end-to-end example -- project-scoped tag key + value, a tag binding on the project, and tag key/value IAM members, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.2
  terradart_google: ^0.25.2

dev_dependencies:
  lints: ^6.0.0
