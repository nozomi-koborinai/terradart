name: terradart_example_cloud_tasks_quickstart
description: terradart example -- Cloud Tasks queue + IAM enqueuer grant, with typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.26.0
  terradart_google: ^0.26.0

dev_dependencies:
  lints: ^6.0.0
