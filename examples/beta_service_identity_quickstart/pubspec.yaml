name: terradart_example_beta_service_identity_quickstart
description: Smallest terradart_google_beta example -- pre-provisions the Pub/Sub service agent via the beta-only google_project_service_identity.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.27.0
  terradart_google_beta: ^0.27.0

dev_dependencies:
  lints: ^6.0.0
