name: terradart_example_vm_compliance_quickstart
description: terradart example — OS Config + Binary Authorization VM compliance.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.21.0
  terradart_google: ^0.21.0

dev_dependencies:
  lints: ^6.0.0
