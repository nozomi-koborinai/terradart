name: terradart_example_bigquery_datapolicyv2_quickstart
description: BigQuery Data Policy V2 quickstart — raw-data access + masking policies with IAM.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.25.2
  terradart_google: ^0.25.2

dev_dependencies:
  lints: ^6.0.0
