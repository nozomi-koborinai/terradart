name: terradart_example_firestore_document_quickstart
description: Cloud Firestore quickstart -- seed a few master-data documents (feature flag, pricing tier) into the default Native-mode database.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.12
  terradart_google: ^0.12.12

dev_dependencies:
  lints: ^6.0.0
