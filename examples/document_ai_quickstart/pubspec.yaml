name: terradart_example_document_ai_quickstart
description: Document AI end-to-end example -- an OCR document processor, with a typed Dart export.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.15.0
  terradart_google: ^0.15.0

dev_dependencies:
  lints: ^6.0.0
