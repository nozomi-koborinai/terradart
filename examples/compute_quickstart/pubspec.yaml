name: terradart_example_compute_quickstart
description: Phase 4.5 Wave 0 quickstart -- VPC network + external IP address with typed Dart enums.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  terradart_core: ^0.12.16
  terradart_google: ^0.12.16

dev_dependencies:
  lints: ^6.0.0
