# Curated factories not yet exercised by any example quickstart.
#
# Policy (AGENTS.md "Wave shipping policy"): every NEW factory must appear in
# an example within its Wave PR, or get a reasoned line here. Pre-existing
# gaps live here explicitly, one `ClassName: reason` line each.
# `dart tool/example_synth_gates.dart` fails when a factory is neither covered
# nor listed, and when a listed factory becomes covered (remove the line) —
# adding a line is a reviewed decision, not a default.
#
# IAM binding/policy (preferred for pure adjunct Waves): when shipping a new
# `*IamBinding` / `*IamPolicy` whose sibling `*IamMember` is already curated
# and already in some quickstart synth, list the new factory here with reason
# containing the token `iam-adjunct-debt:` (plus a short human note). The synth
# gate enforces sibling presence — no example change and no per-PR apply-smoke.
# Example:
#   GoogleFooIamBinding: iam-adjunct-debt: sibling GoogleFooIamMember already in bar_quickstart; authoritative binding not needed in smoke

# Monitoring factories that cannot `terraform apply` against a standalone
# apply-smoke project, so monitoring_quickstart uses the applyable variants
# instead (the custom-service resource) and drops the self-linking monitored
# project. Tracked here rather than forced into an example that would fail the
# apply-smoke sweep.
GoogleMonitoringService: typed google_monitoring_service needs a basic_service identifier bound to a real well-known service (App Engine / Istio / etc.) absent from a bare project; the applyable GoogleMonitoringCustomService is exercised by monitoring_quickstart instead
GoogleMonitoringMonitoredProject: links another project into a metrics scope — self-linking the scoping project is rejected, and cross-project linking needs a second project plus roles/monitoring.admin on both, which a standalone apply-smoke project can't provide

# GKE Backup channels are inherently cross-project: a channel connects a source
# project to a *different* destination project. In a standalone apply-smoke
# project source==dest, which the API rejects with "source and destination
# project cannot be same". Exercising them needs a second project, so
# gke_quickstart drops them (it keeps the applyable backup/restore plans).
GoogleGkeBackupBackupChannel: connects a source project to a different destination project; with only one project source==dest and creation fails 400 "source and destination project cannot be same". Needs a second project an apply-smoke run can't provide
GoogleGkeBackupRestoreChannel: same cross-project constraint as GoogleGkeBackupBackupChannel — destination project must differ from the source, which a single standalone apply-smoke project cannot satisfy

# Every project has exactly one fleet, auto-created on first GKE Hub use, so
# creating google_gke_hub_fleet for the default fleet always fails 409
# "Resource '.../fleets/default' already exists". gke_quickstart enrolls the
# cluster via GoogleGkeHubMembership against the auto-created default fleet
# instead of creating the fleet resource.
GoogleGkeHubFleet: the project default fleet is auto-created, so applying google_gke_hub_fleet returns 409 "fleets/default already exists"; there is no second fleet to create in a standalone project and the provider exposes no data source to adopt the existing one

# google_firestore_user_creds creates a database-level credential for a Cloud
# Firestore with MongoDB compatibility (Enterprise-edition) database. Applying
# it against a Standard-edition database fails 400 "This operation requires an
# Enterprise database." firestore_quickstart deliberately uses a Standard-edition
# named database (quickstart-db) so it can be created/destroyed repeatably;
# switching to Enterprise would change the example's intent (a heavier,
# cost-different product), so the resource is dropped from the example instead.
GoogleFirestoreUserCreds: requires an Enterprise-edition (MongoDB-compatible) Firestore database; firestore_quickstart uses a Standard-edition named database, against which creation fails 400 "This operation requires an Enterprise database." Switching the database to Enterprise would change the example's intent, so the resource is dropped

# Setting IAM on a restore plan rejected the binding with 400 "Invalid argument"
# at the restore-plan resource scope. GoogleGkeBackupBackupPlanIamMember already
# demonstrates GKE Backup resource-level IAM in gke_quickstart, so the redundant
# per-restore-plan binding is dropped rather than chased further.
GoogleGkeBackupRestorePlanIamMember: resource-level setIamPolicy on a restore plan failed 400 "Invalid argument"; the sibling GoogleGkeBackupBackupPlanIamMember already covers GKE Backup resource-level IAM in gke_quickstart, so this binding is dropped

# Regional Secret Manager secrets have no write-only (`_wo`) data field, and
# synth refuses a literal on the sensitive `secret_data` field (v1.0 no longer
# masks sensitive literals). Supplying the value via a Terraform variable would
# make secret_manager_quickstart (otherwise applyable) require `-var` at apply
# time and force it onto the apply-smoke skip list. The regional secret +
# regional secret IAM member are exercised in secret_manager_quickstart; only
# the version factory is deferred here.
GoogleSecretManagerRegionalSecretVersion: regional secrets lack a write-only data field and synth rejects a literal on the sensitive secret_data field; exercising it needs a Terraform variable that would break the applyable secret_manager_quickstart, so the version factory is tracked here while the regional secret + IAM member ship in the example

# Parameter Manager parameter versions hold the payload in the sensitive
# `parameter_data` field. As with regional Secret Manager versions, synth v1.0
# refuses a literal on a sensitive field, and supplying it via a Terraform
# variable would make parameter_manager_quickstart (otherwise applyable)
# require `-var` at apply time. The global + regional parameter factories ship
# in parameter_manager_quickstart; only the version factories are deferred here.
GoogleParameterManagerParameterVersion: parameter versions carry the payload in the sensitive parameter_data field; synth rejects a literal there and a Terraform variable would break the applyable parameter_manager_quickstart, so the version factory is tracked here while the global parameter ships in the example
GoogleParameterManagerRegionalParameterVersion: regional parameter versions carry the payload in the sensitive parameter_data field; same constraint as the global version, so the regional version factory is tracked here while the regional parameter ships in parameter_manager_quickstart

# Compute networking factories that cannot `terraform apply` against a
# standalone apply-smoke project. compute_quickstart keeps the applyable
# surface (VPC / subnet / router / NAT / address / instance / disk /
# filestore / IAM) and drops these because none can be provisioned
# deterministically in the sweep.
GoogleComputeSharedVpcHostProject: enabling a Shared VPC host needs an organization — "The project has no organization" on a standalone apply-smoke project
GoogleComputeSharedVpcServiceProject: attaches a service project to a Shared VPC host, which requires an organization the standalone apply-smoke project lacks
GoogleComputeInterconnect: a Dedicated Interconnect needs a real physical cross-connect (a provisioned circuit / customerName); it cannot be created in a bare project
GoogleComputeInterconnectAttachment: a VLAN attachment requires a provisioned Interconnect, which needs a physical circuit absent from the apply-smoke project
GoogleComputeExternalVpnGateway: only useful paired with a VPN tunnel to an external peer; the tunnels are dropped, so the external gateway is dropped with them
GoogleComputeVpnGateway: a classic target VPN gateway needs ESP/UDP forwarding rules before a tunnel can be created; that scaffolding is out of scope for the applyable example
GoogleComputeHaVpnGateway: HA VPN tunnels need a Cloud Router with BGP sessions; the extra interface/peer wiring is out of scope for the applyable example
GoogleComputeVpnTunnel: VPN tunnels require a configured gateway (forwarding rules for classic, Cloud Router BGP for HA), neither of which can be provisioned deterministically in the sweep
GoogleComputeNetworkPeering: bidirectional VPC peering races on GCP's async peering operation ("peering operation in progress") even when serialized, so it cannot apply deterministically
GoogleComputeNetworkPeeringRoutesConfig: configures routes on a VPC peering; with the peering dropped there is nothing to attach it to

# Identity Platform project config is a singleton. Shared validate projects that
# already enabled Identity Platform reject create with 400 "already been enabled".
# Tenant apply also fails on terradart-validate (400 INVALID_PROJECT_ID) — the
# quickstart stays synth-only via tool/apply_smoke_skip.yaml.
GoogleIdentityPlatformConfig: project singleton — terradart-validate (and any project with Identity Platform already on) rejects create with 400 "Identity Platform has already been enabled"; use the factory on a fresh project or import the existing config

# Container Analysis occurrences require a base64 attestation payload + signature
# (typically KMS / Binary Authorization). container_analysis_quickstart covers
# the note + note IAM member instead.
GoogleContainerAnalysisOccurrence: attestation occurrences need a real signed serialized_payload and public_key_id (KMS or similar); placeholder material fails apply — extend the quickstart when a deterministic signing path exists in terradart-validate

# App Hub service/workload registration needs a second (service) project,
# service-project attachment, and discovered_* data-source URIs. apphub_quickstart
# covers the host boundary + application instead.
GoogleApphubServiceProjectAttachment: attaches a separate service project to the App Hub host boundary; terradart-validate is a single standalone project and cannot create an org-scoped sibling project for attachment
GoogleApphubService: registers a discovered service URI under an application; needs service-project attachment + google_apphub_discovered_service (data source not curated / needs live Compute URI)
GoogleApphubWorkload: registers a discovered workload URI under an application; needs service-project attachment + google_apphub_discovered_workload (data source not curated / needs live MIG URI)

# Healthcare Data Mapper workspace create returns 404 Method not found against
# the GA Healthcare API on terradart-validate (us-central1). healthcare_quickstart
# keeps the applyable dataset/stores/IAM surface; revisit when the Data Mapper
# API method is available in this project/region.
GoogleHealthcareWorkspace: google_healthcare_workspace create returns 404 Method not found on terradart-validate (GA Healthcare API / us-central1); Data Mapper may require entitlement or a different API surface — healthcare_quickstart keeps applyable stores instead
GoogleBigqueryReservationGroup: create returns 400 "Can not use Reservation Groups without Reservation Based Fairness enabled on the project"; enabling it needs ALTER PROJECT SET OPTIONS (`region-LOCATION.enable_reservation_based_fairness`=true) outside Terraform apply-smoke scope on terradart-validate

# SCC v2 project notification configs create an org-scoped notification service
# agent (service-org-ORGANIZATION_ID@gcp-sa-scc-notification.iam.gserviceaccount.com).
# terradart-validate is a standalone project with no organization parent
# (gcloud projects get-ancestors → project only), so create returns 400
# "Precondition check failed." Keep the curated factory; revisit when an
# org-backed validate project exists.
GoogleSccV2ProjectNotificationConfig: requires an organization-activated Security Command Center parent — notification configs mint service-org-ORGANIZATION_ID@gcp-sa-scc-notification.iam.gserviceaccount.com; terradart-validate has no org ancestor, so apply returns 400 Precondition check failed

# Vector Search indexes default dedicated_infrastructure to two
# PERFORMANCE_OPTIMIZED replicas (hourly capacity-unit SKUs). vector_quickstart
# exercises the schema-only collection instead.
GoogleVectorSearchIndex: API defaults dedicated_infrastructure to PERFORMANCE_OPTIMIZED with min/max_replica_count=2 (Vector Search V2 capacity units billed hourly, e.g. SKU 2ECA-1449-C093 $0.065/h); including the index would force apply_smoke_pr_skip and block wave auto-merge — vector_quickstart covers GoogleVectorSearchCollection only

# Secure Source Manager instances are existence-billed at ~$1000/mo (Fixed
# Instance Pricing). A quickstart would require apply_smoke_skip (never_apply)
# and block wave auto-merge — factories ship without an example.
GoogleSecureSourceManagerInstance: gcp-cost Fixed Instance Pricing us-central1 SKU 9B40-B4AA-D8EE $1000/mo — never_apply; no applyable quickstart on terradart-validate
GoogleSecureSourceManagerRepository: requires a parent GoogleSecureSourceManagerInstance (never_apply); deferred with the instance
GoogleSecureSourceManagerInstanceIamMember: requires a parent GoogleSecureSourceManagerInstance (never_apply); deferred with the instance
GoogleSecureSourceManagerRepositoryIamMember: requires a parent repository on a never_apply instance; deferred with the instance

# Managed Service for Apache Kafka clusters bill Data Compute Units hourly
# (us-central1 SKU 2837-8C0C-F431 $0.09/h) plus storage; Connect clusters bill
# Connect DCU (12FB-6837-84CF $0.12/h). A quickstart would force
# apply_smoke_pr_skip and block wave auto-merge — factories ship without an example.
GoogleManagedKafkaCluster: gcp-cost Managed Kafka 9544-7B1C-811D Data Compute Units us-central1 SKU 2837-8C0C-F431 $0.09/h + Local Storage 1533-0F96-68AD $0.17/GiBy.mo — sweep_only capacity; no applyable quickstart on terradart-validate
GoogleManagedKafkaTopic: requires a parent GoogleManagedKafkaCluster (sweep_only capacity); deferred with the cluster
GoogleManagedKafkaAcl: requires a parent GoogleManagedKafkaCluster (sweep_only capacity); deferred with the cluster
GoogleManagedKafkaConnectCluster: gcp-cost Managed Kafka Connect DCU us-central1 SKU 12FB-6837-84CF $0.12/h — sweep_only; deferred without apply-smoke quickstart
GoogleManagedKafkaConnector: requires a parent GoogleManagedKafkaConnectCluster (sweep_only); deferred with Connect

# Google Cloud NetApp Volumes storage pools bill capacity while they exist
# (us-central1 Standard SKU C2DF-4710-FFE1 $0.2/GiBy.mo; typical min TiB-scale).
# A quickstart would force apply_smoke_skip (never_apply) and block wave
# auto-merge — factories ship without an example.
GoogleNetappStoragePool: gcp-cost NetApp Volumes FC86-5113-7C81 Capacity Standard us-central1 SKU C2DF-4710-FFE1 $0.2/GiBy.mo (Flex 211D-EBE1-87C9 $0.2/GiBy.mo; Premium 5BCD-5BCB-41A3 ~$0.29/GiBy.mo) — never_apply capacity; no applyable quickstart on terradart-validate
GoogleNetappVolume: requires a parent GoogleNetappStoragePool (never_apply capacity); deferred with the pool
GoogleNetappVolumeSnapshot: requires a parent GoogleNetappVolume on a never_apply pool; deferred with the pool
GoogleNetappBackupVault: deferred with the NetApp pool Wave (no apply-smoke quickstart); empty vault has no capacity charge but pool Wave is debt-only
GoogleNetappBackupPolicy: deferred with the NetApp pool Wave (policy metadata only; backup storage bills when backups exist)
GoogleNetappActiveDirectory: deferred with the never_apply NetApp pool Wave (AD policy metadata; no apply-smoke quickstart)
GoogleNetappBackup: gcp-cost NetApp Volumes FC86-5113-7C81 Backup Storage Charge us-central1 SKU DCB6-FE72-5443 $0.045/GiBy.mo — never_apply stored backup bytes + never_apply pool parent; no applyable quickstart on terradart-validate
GoogleNetappHostGroup: deferred with the never_apply NetApp pool Wave (host membership metadata; no apply-smoke quickstart)
GoogleNetappKmsconfig: deferred with the never_apply NetApp pool Wave (CMEK policy metadata; no apply-smoke quickstart)
GoogleNetappVolumeQuotaRule: requires a parent GoogleNetappVolume on a never_apply pool; deferred with the pool
GoogleNetappVolumeReplication: gcp-cost NetApp Volumes FC86-5113-7C81 Inter Zone Replication Iowa SKU 1432-4402-7168 $0.11/GiBy (External 832F-38DB-A8C1 $0.11/GiBy) — never_apply replication + never_apply pool; no applyable quickstart on terradart-validate

# Cloud Workstations clusters bill a control-plane fee while they exist
# (us-central1 SKU 61C4-0757-3151 $0.2/h). A quickstart would force
# apply_smoke_skip (never_apply) and block wave auto-merge — factories
# ship without an example.
GoogleWorkstationsWorkstationCluster: gcp-cost Cloud Workstations 4528-FDD0-A2A0 control plane fee us-central1 SKU 61C4-0757-3151 $0.2/h — never_apply; no applyable quickstart on terradart-validate
GoogleWorkstationsWorkstationConfig: requires a parent GoogleWorkstationsWorkstationCluster (never_apply); deferred with the cluster
GoogleWorkstationsWorkstation: gcp-cost Cloud Workstations instance VM (vCPU) management fee us-central1 SKU 8893-953D-5524 $0.05/h (+ Compute) — requires never_apply cluster; deferred with the cluster
GoogleWorkstationsWorkstationConfigIamMember: requires a parent workstation config on a never_apply cluster; deferred with the cluster
GoogleWorkstationsWorkstationIamMember: requires a parent workstation on a never_apply cluster; deferred with the cluster

# Cloud Composer environments bill an environment fee while they exist
# (us-central1 Small SKU 6EA4-3652-173E $0.35/h). A quickstart would force
# apply_smoke_skip (never_apply) and block wave auto-merge — factories
# ship without an example.
GoogleComposerEnvironment: gcp-cost Cloud Composer 1992-3666-B975 Small Environment Fee us-central1 SKU 6EA4-3652-173E $0.35/h (Medium 9802-34D7-AFE2 $0.55/h; Large 2C11-0523-61D9 $0.85/h) — never_apply; no applyable quickstart on terradart-validate
GoogleComposerUserWorkloadsConfigMap: requires a parent GoogleComposerEnvironment (never_apply); deferred with the environment
GoogleComposerUserWorkloadsSecret: requires a parent GoogleComposerEnvironment (never_apply); deferred with the environment

# Memorystore for Redis Cluster bills per node-hour while the cluster exists
# (us-central1 Shared Core Nano SKU B4D9-BD1A-3BBC $0.0318/h × shards ×
# replicas). A quickstart would force apply_smoke_skip (never_apply) and
# block wave auto-merge — factories ship without an example.
GoogleRedisCluster: gcp-cost Memorystore for Redis 5AF5-2C11-D467 Shared Core Nano us-central1 SKU B4D9-BD1A-3BBC $0.0318/h (Standard Small 02F6-0CB2-BDE1 $0.1425/h) — never_apply node-hours; no applyable quickstart on terradart-validate
GoogleRedisClusterUserCreatedConnections: requires a parent GoogleRedisCluster (never_apply); deferred with the cluster

# Looker (Google Cloud core) bills a platform fee while the instance exists
# (Standard Edition SKU 3F43-B8CB-2533 $5000/count). A quickstart would force
# apply_smoke_skip (never_apply) and block wave auto-merge — factories ship
# without an example.
GoogleLookerInstance: gcp-cost Looker C71C-0952-AAC7 Standard Edition SKU 3F43-B8CB-2533 $5000/count — never_apply platform fee; no applyable quickstart on terradart-validate

# Cloud Data Fusion bills instance hours by edition (Developer 88BC-986D-9A22
# $0.35/h; Basic E74D-F7A2-0BF5 $1.8/h after free tier; Enterprise
# 37C9-6893-468A $4.2/h). A quickstart would force apply_smoke_skip
# (never_apply) and block wave auto-merge — factory ships without an example.
GoogleDataFusionInstance: gcp-cost Cloud Data Fusion 0D19-EC86-35B0 Developer SKU 88BC-986D-9A22 $0.35/h (Basic E74D-F7A2-0BF5 $1.8/h after 120 free h; Enterprise 37C9-6893-468A $4.2/h) — never_apply instance hours; no applyable quickstart on terradart-validate

# Managed Lustre bills provisioned capacity while the instance exists
# (125-Perf us-central1 SKU 517C-E77D-C11A $0.145/GiBy.mo). A quickstart would
# force apply_smoke_skip (never_apply) and block wave auto-merge — factory
# ships without an example.
GoogleComputeInterconnectAttachmentGroup: gcp-cost Compute Engine 6F81-5844-456A Cloud Interconnect 10Gbps Dedicated circuit SKU B8C8-2F76-E648 $2.328/h — never_apply physical interconnect family; no applyable quickstart on terradart-validate
GoogleComputeInterconnectGroup: gcp-cost Compute Engine 6F81-5844-456A Cloud Interconnect 10Gbps Dedicated circuit SKU B8C8-2F76-E648 $2.328/h — never_apply physical interconnect family; no applyable quickstart on terradart-validate
GoogleComputeCrossSiteNetwork: gcp-cost NCC 7BEB-7A51-4223 Partner Cross Cloud Interconnect Managed Transport 10Gbps SKU AAE5-BD60-3575 $17.30/h — never_apply; no applyable quickstart
GoogleComputeWireGroup: deferred with never_apply GoogleComputeCrossSiteNetwork (CCI / cross-site path)
GoogleComputePacketMirroring: gcp-cost Compute Engine 6F81-5844-456A Packet Mirroring Data Processing Americas SKU A0DF-D169-F1EE $0.008/GiBy — never_apply; no applyable quickstart
GoogleNetworkSecurityMirroringEndpoint: deferred with never_apply GoogleNetworkSecurityMirroringDeployment (OOB mirroring path)
GoogleNetworkSecurityMirroringEndpointGroup: deferred with never_apply GoogleNetworkSecurityMirroringDeployment (OOB mirroring path)
GoogleNetworkSecurityMirroringEndpointGroupAssociation: deferred with never_apply GoogleNetworkSecurityMirroringDeployment (OOB mirroring path)
GoogleNetworkConnectivityMulticloudDataTransferConfig: gcp-cost NCC 7BEB-7A51-4223 Partner Cross Cloud Interconnect Managed Transport 10Gbps SKU AAE5-BD60-3575 $17.30/h — never_apply; no applyable quickstart
# Cloud Multicast (allowlisted GA) — infrastructure reservation billed from
# domain activation (docs; no Cloud Billing Catalog SKU). Factories ship without
# an example — never_apply.
GoogleNetworkServicesMulticastDomain: gcp-cost no Cloud Billing Catalog SKU (Networking E505-1604-58F8 multicast → 0); docs cloud.google.com/vpc/docs/multicast — infra billed from domain activation; never_apply; no applyable quickstart
GoogleNetworkServicesMulticastDomainGroup: deferred with never_apply GoogleNetworkServicesMulticastDomain (Cloud Multicast path)
GoogleNetworkServicesMulticastDomainActivation: gcp-cost no Cloud Billing Catalog SKU; docs — billing starts when domain activation is created; never_apply; no applyable quickstart
GoogleNetworkServicesMulticastGroupRange: deferred with never_apply GoogleNetworkServicesMulticastDomain (Cloud Multicast path)
GoogleNetworkServicesMulticastGroupRangeActivation: deferred with never_apply GoogleNetworkServicesMulticastDomainActivation (Cloud Multicast path)
GoogleNetworkServicesMulticastConsumerAssociation: deferred with never_apply GoogleNetworkServicesMulticastDomainActivation (Cloud Multicast path)
GoogleNetworkServicesMulticastProducerAssociation: deferred with never_apply GoogleNetworkServicesMulticastDomainActivation (Cloud Multicast path)
GoogleNetworkServicesMulticastGroupConsumerActivation: deferred with never_apply GoogleNetworkServicesMulticastDomainActivation (Cloud Multicast path)
GoogleNetworkServicesMulticastGroupProducerActivation: deferred with never_apply GoogleNetworkServicesMulticastDomainActivation (Cloud Multicast path)
GoogleNetworkConnectivityDestination: requires never_apply GoogleNetworkConnectivityMulticloudDataTransferConfig (CCI / multicloud data-transfer path); deferred with that config
GoogleContainerAttachedCluster: gcp-cost GKE Enterprise / GDC 9186-F79E-3871 Trial Attached SKU CA50-C2AE-45E8 $0/h (catalog); needs real external cluster — never_apply; no applyable quickstart on terradart-validate
GoogleContainerAwsCluster: gcp-cost GKE Enterprise / GDC 9186-F79E-3871 Multicloud (AWS) SKU 24A0-2EF1-8ACB $0.00822/h (+ AWS EC2) — never_apply; needs real AWS account
GoogleContainerAwsNodePool: requires never_apply GoogleContainerAwsCluster (+ AWS EC2); deferred with the AWS cluster
GoogleContainerAzureClient: requires never_apply GoogleContainerAzureCluster family; deferred with the Azure cluster
GoogleContainerAzureCluster: gcp-cost GKE Enterprise / GDC 9186-F79E-3871 Multicloud (Azure) SKU 688E-3D16-399E $0.00822/h (+ Azure VMs) — never_apply; needs real Azure tenant
GoogleContainerAzureNodePool: requires never_apply GoogleContainerAzureCluster (+ Azure VMs); deferred with the Azure cluster
GooglePubsubLiteReservation: gcp-cost Pub/Sub Lite 3A1B-66C4-2BAE Reservation Throughput Capacity Allocated North America SKU 0E37-3B5B-8275 $0.00625/h (Default 576D-ADC9-9995 $0.008/h) — never_apply reserved throughput; no applyable quickstart on terradart-validate
GooglePubsubLiteTopic: gcp-cost Pub/Sub Lite 3A1B-66C4-2BAE Publisher Throughput Capacity Allocated North America SKU CE6F-6EC1-8463 $0.00625/MiBy.h (+ Subscriber DB2E-0351-AE15 $0.003125/MiBy.h; Storage 13A4-C375-427E $0.04/GiBy.mo) — never_apply; deferred with reservation
GooglePubsubLiteSubscription: requires never_apply GooglePubsubLiteTopic / GooglePubsubLiteReservation; deferred with the Lite Wave
GoogleHypercomputeclusterCluster: gcp-cost no Cluster Director SKU (list_services empty); Cloud TPU E000-3F24-B8AA TPU-v2 USA SKU 3B3D-4CB4-AECC $4.5/h — never_apply HPC capacity; no applyable quickstart on terradart-validate
GoogleComputeNodeGroup: gcp-cost Compute Engine 6F81-5844-456A N4A Sole Tenancy Instance Core Iowa SKU 6DD8-C2A8-A106 $0.02646/h (+ Premium 0F1E-4428-FCCB $0.002646/h) — never_apply dedicated nodes; no applyable quickstart on terradart-validate
GoogleComputeNodeTemplate: gcp-cost Compute Engine 6F81-5844-456A sole-tenancy SKUs bill via node groups (e.g. N4A Core Iowa 6DD8-C2A8-A106 $0.02646/h) — never_apply family; deferred with GoogleComputeNodeGroup
GoogleComputeReservation: gcp-cost Compute Engine 6F81-5844-456A N4A Custom Instance Core Iowa SKU F179-06F3-16EC $0.027783/h (+ 1/8 vGPU Iowa D5C5-C5F4-086C $0.64688/h) — never_apply reserved capacity bills while reservation exists; no applyable quickstart on terradart-validate
GoogleComputeRegionCommitment: gcp-cost Compute Engine 6F81-5844-456A Commitment v1 Cpu Virginia 1 Year SKU 00EE-95C9-2FF9 $0.021309/h — never_apply undeletable commitment contract; no applyable quickstart on terradart-validate
GoogleEdgecontainerCluster: gcp-cost Google Distributed Cloud Edge 8A2D-5CB1-345B Connected Server Gen1 SKU 007E-2D86-E472 $3600/mo — never_apply; requires physical GDCE hardware absent on terradart-validate
GoogleGkeonpremBareMetalAdminCluster: gcp-cost GKE Enterprise/GDC 9186-F79E-3871 Bare Metal SKU 297F-4642-B7A1 $0.03288/h — never_apply; requires physical bare-metal hardware absent on terradart-validate
GoogleGkeonpremBareMetalCluster: gcp-cost GKE Enterprise/GDC 9186-F79E-3871 Bare Metal SKU 297F-4642-B7A1 $0.03288/h — never_apply; requires physical bare-metal hardware absent on terradart-validate
GoogleGkeonpremBareMetalNodePool: requires never_apply GoogleGkeonpremBareMetalCluster (+ physical hardware); deferred with the GKE on-prem Wave
GoogleGkeonpremVmwareAdminCluster: gcp-cost GKE Enterprise/GDC 9186-F79E-3871 vSphere SKU 82D9-AB10-CA55 $0.03288/h — never_apply; requires real vSphere environment absent on terradart-validate
GoogleGkeonpremVmwareCluster: gcp-cost GKE Enterprise/GDC 9186-F79E-3871 vSphere SKU 82D9-AB10-CA55 $0.03288/h — never_apply; requires real vSphere environment absent on terradart-validate
GoogleGkeonpremVmwareNodePool: requires never_apply GoogleGkeonpremVmwareCluster (+ vSphere); deferred with the GKE on-prem Wave
GoogleEdgecontainerNodePool: gcp-cost Google Distributed Cloud Edge 8A2D-5CB1-345B Connected Server Gen1 SKU 007E-2D86-E472 $3600/mo — never_apply; requires parent GDCE cluster + physical hardware absent on terradart-validate
GoogleEdgecontainerVpnConnection: gcp-cost Google Distributed Cloud Edge 8A2D-5CB1-345B Connected Server Gen1 SKU 007E-2D86-E472 $3600/mo — never_apply; requires parent GDCE cluster absent on terradart-validate
GoogleEdgenetworkInterconnectAttachment: gcp-cost Google Distributed Cloud Edge 8A2D-5CB1-345B Connected Server Gen1 SKU 007E-2D86-E472 $3600/mo — never_apply; requires physical GDCE interconnect absent on terradart-validate
GoogleEdgenetworkNetwork: gcp-cost Google Distributed Cloud Edge 8A2D-5CB1-345B Connected Server Gen1 SKU 007E-2D86-E472 $3600/mo — never_apply; requires physical GDCE zone absent on terradart-validate
GoogleEdgenetworkSubnet: gcp-cost Google Distributed Cloud Edge 8A2D-5CB1-345B Connected Server Gen1 SKU 007E-2D86-E472 $3600/mo — never_apply; requires parent edge network + physical zone absent on terradart-validate
GoogleLustreInstance: gcp-cost Managed Lustre B384-1FDE-A709 Capacity 125-Perf us-central1 SKU 517C-E77D-C11A $0.145/GiBy.mo — never_apply capacity; no applyable quickstart on terradart-validate

# Parallelstore bills provisioned capacity while the instance exists
# (Scratch us-central1 SKU 920E-C157-D1E5 $0.000191781/GiBy.h). A quickstart
# would force apply_smoke_skip (never_apply) and block wave auto-merge —
# factory ships without an example.
GoogleParallelstoreInstance: gcp-cost Parallelstore 3450-1C42-A427 Scratch us-central1 SKU 920E-C157-D1E5 $0.000191781/GiBy.h (Persistent AC48-A917-C907 $0.000821917/GiBy.h) — never_apply capacity; no applyable quickstart on terradart-validate

# Apigee API Hub is a project singleton with exclude_delete (Terraform cannot
# destroy). No Cloud Billing Catalog SKU after MCP lookup. A quickstart would
# strand the validate project — factory ships without an example.
GoogleApihubApiHubInstance: gcp-cost no Cloud Billing Catalog SKU (list_services API Hub/apihub empty; Apigee 1C2D-8C78-EC58 Hub → 0) — never_apply undeletable project singleton; no applyable quickstart on terradart-validate

# Vertex AI Workbench notebook VMs bill management fee + GCE while running
# (Notebooks D73B-5EEA-8215 N1 mgmt CPU us-central1 SKU 6196-1C27-5E30
# $0.0063222/h + usage). A quickstart would force apply_smoke_skip
# (never_apply) and block wave auto-merge — factories ship without an example.
GoogleNotebooksInstance: gcp-cost Notebooks D73B-5EEA-8215 N1 mgmt CPU us-central1 SKU 6196-1C27-5E30 $0.0063222/h (+ usage CPU 9027-15FF-8BFE $0.0379332/h) — never_apply VM hours; no applyable quickstart on terradart-validate
GoogleNotebooksRuntime: gcp-cost Notebooks D73B-5EEA-8215 same Workbench management fee + GCE as instances — never_apply; deferred with notebook instance Wave
GoogleNotebooksInstanceIamMember: requires a parent GoogleNotebooksInstance (never_apply); deferred with the instance
GoogleNotebooksRuntimeIamMember: requires a parent GoogleNotebooksRuntime (never_apply); deferred with the runtime

# Colab Enterprise runtime assigns a VM from a template (Vertex Colab CPU/RAM
# SKUs while running). A quickstart would force apply_smoke_skip (never_apply)
# and block wave auto-merge — factory ships without an example.
GoogleColabRuntime: gcp-cost Vertex AI C7E2-9256-1C43 Colab N2 CPU usage us-central1 SKU 7362-581B-29B4 $0.0379332/h (+ E2 RAM 9215-A98F-C4CD $0.003508236/GiBy.h) — never_apply VM hours; no applyable quickstart on terradart-validate
GoogleColabNotebookExecution: gcp-cost Vertex AI C7E2-9256-1C43 Colab N2 CPU usage us-central1 SKU 7362-581B-29B4 $0.0379332/h (+ E2 RAM 9215-A98F-C4CD $0.003508236/GiBy.h) — never_apply execution compute; no applyable quickstart on terradart-validate
GoogleActiveDirectoryDomain: gcp-cost Managed Microsoft AD 2A27-8988-64B8 SKU BE02-3DF8-6AB6 $0.4/h — never_apply domain hours; no applyable quickstart on terradart-validate
GoogleStorageAnywhereCache: gcp-cost Cloud Storage 95FF-2EF5-5EA1 Rapid Cache Storage Iowa SKU A668-3CA8-42C8 $0.0001233/GiBy.h — never_apply cache capacity; no applyable quickstart on terradart-validate

# Backup and DR vaults/management bill storage + protected-resource management
# (BackupDR 3DAD-299B-0D94 storage SKU 5A13-2468-31B1 $0.045/GiBy.mo; GCE
# management 0456-5BF2-438E $0.02/GiBy.mo). A quickstart would force
# apply_smoke_skip (never_apply) and block wave auto-merge — factories ship
# without an example.
GoogleBackupDrManagementServer: gcp-cost BackupDR 3DAD-299B-0D94 GCE VM management SKU 0456-5BF2-438E $0.02/GiBy.mo (+ appliance Compute) — never_apply; no applyable quickstart on terradart-validate
GoogleBackupDrBackupVault: gcp-cost BackupDR 3DAD-299B-0D94 Long-Term Standard us-central1 SKU 5A13-2468-31B1 $0.045/GiBy.mo — never_apply vault storage; deferred with management server Wave
GoogleBackupDrBackupPlan: requires a parent GoogleBackupDrBackupVault (never_apply); deferred with the vault
GoogleBackupDrBackupPlanAssociation: requires a parent plan/vault (never_apply); deferred with the vault — associating starts management fees
GoogleBackupDrServiceConfig: deferred with the Backup DR Wave (config metadata; no apply-smoke quickstart)
GoogleBackupDrRestoreWorkload: requires vault backups (never_apply parents); deferred with the vault — restore materializes Compute/Disk

# Dataproc classic clusters materialize GCE VMs while they exist (Cloud
# Billing Catalog Dataproc 363B-8851-170D lists Serverless SKUs only after
# MCP lookup; Batch DCU EC7A-EF05-537E $0.06/h). A quickstart would force
# apply_smoke_skip (never_apply) and block wave auto-merge — factories
# ship without an example.
GoogleDataprocCluster: gcp-cost Dataproc 363B-8851-170D Serverless Batch DCU us-central1 SKU EC7A-EF05-537E $0.06/h (Interactive DCU A486-6040-07FE $0.089/h; no classic cluster premium SKU in catalog) — never_apply GCE+premium hours; no applyable quickstart on terradart-validate
GoogleDataprocClusterIamMember: requires a parent GoogleDataprocCluster (never_apply); deferred with the cluster
GoogleDataprocJob: gcp-cost Dataproc 363B-8851-170D Serverless Batch DCU us-central1 SKU EC7A-EF05-537E $0.06/h (no classic job premium SKU in catalog) — never_apply; job workers on cluster / ephemeral GCE; no applyable quickstart on terradart-validate

# Dataproc-on-GDC needs a real GDCE cluster (edge/appliance). GDC Bare Metal
# SKU 297F-4642-B7A1 $0.03288/h. A quickstart would force apply_smoke_skip
# (never_apply) and block wave auto-merge — factory ships without an example.
GoogleDataprocGdcServiceInstance: gcp-cost GKE Enterprise/GDC 9186-F79E-3871 Bare Metal SKU 297F-4642-B7A1 $0.03288/h (vSphere 82D9-AB10-CA55 $0.03288/h) — never_apply; requires GDCE cluster absent on terradart-validate
GoogleDataprocSessionTemplate: gcp-cost Dataproc 363B-8851-170D Interactive DCU SKU A486-6040-07FE $0.089/h — never_apply; no applyable quickstart without Interactive compute
GoogleDataprocBatch: gcp-cost Dataproc 363B-8851-170D Serverless Batch DCU us-central1 SKU EC7A-EF05-537E $0.06/h (Premium DCU C275-E37B-D8BA $0.089/h) — never_apply DCU-hours; no applyable quickstart on terradart-validate
GoogleDataprocGdcApplicationEnvironment: requires never_apply GoogleDataprocGdcServiceInstance (GDC platform); deferred with the GDC service-instance Wave

# Vertex AI Workbench instances bill management fee + GCE while running
# (Notebooks D73B-5EEA-8215 N1 mgmt CPU us-central1 SKU 6196-1C27-5E30
# $0.0063222/h + usage). A quickstart would force apply_smoke_skip
# (never_apply) and block wave auto-merge — factories ship without an example.
GoogleVmwareenginePrivateCloud: gcp-cost VMware Engine C079-64FE-9109 Gen 2 Standard 112 VCPU Node us-west2 SKU 00C9-4870-5751 $15.11/h — never_apply multi-node SDDC; no applyable quickstart on terradart-validate
GoogleVmwareengineCluster: gcp-cost VMware Engine C079-64FE-9109 Gen 2 Standard 112 VCPU Node us-west2 SKU 00C9-4870-5751 $15.11/h — never_apply additional cluster nodes; requires GoogleVmwareenginePrivateCloud
GoogleVmwareengineDatastore: gcp-cost VMware Engine C079-64FE-9109 list_skus keyword datastore → 0; Filestore/NetApp + private-cloud node hours — never_apply family; deferred with GoogleVmwareenginePrivateCloud
GoogleVmwareengineExternalAccessRule: requires network policy / never_apply private-cloud family; deferred with GoogleVmwareenginePrivateCloud
GoogleVmwareengineExternalAddress: requires never_apply private-cloud family; deferred with GoogleVmwareenginePrivateCloud
GoogleVmwareengineSubnet: gcp-cost VMware Engine C079-64FE-9109 list_skus keyword subnet → 0; requires never_apply GoogleVmwareenginePrivateCloud — deferred with the private cloud
GoogleVmwareengineNetwork: gcp-cost VMware Engine C079-64FE-9109 list_skus keyword network → 0; private-cloud node hours SKU 00C9-4870-5751 $15.11/h — never_apply family (no meaningful quickstart without private cloud); deferred with GoogleVmwareenginePrivateCloud
GoogleVmwareengineNetworkPolicy: requires a parent GoogleVmwareengineNetwork (never_apply family); deferred with the VMware Engine private cloud
GoogleVmwareengineNetworkPeering: requires a parent GoogleVmwareengineNetwork (never_apply family); deferred with the VMware Engine private cloud
GoogleWorkbenchInstance: gcp-cost Notebooks D73B-5EEA-8215 N1 mgmt CPU us-central1 SKU 6196-1C27-5E30 $0.0063222/h (+ usage CPU 9027-15FF-8BFE $0.0379332/h) — never_apply VM hours; no applyable quickstart on terradart-validate
GoogleWorkbenchInstanceIamMember: requires a parent GoogleWorkbenchInstance (never_apply); deferred with the instance

# Memorystore for Valkey bills per node-hour while the instance exists
# (Cloud Memorystore A2B5-E0F1-B0F3 Shared Core Nano us-central1 SKU
# 1E97-7EC7-226D $0.0318/h). A quickstart would force apply_smoke_skip
# (never_apply) and block wave auto-merge — factories ship without an example.
GoogleMemorystoreInstance: gcp-cost Memorystore A2B5-E0F1-B0F3 Shared Core Nano us-central1 SKU 1E97-7EC7-226D $0.0318/h (Standard Small 7054-E9DE-8B60 $0.1425/h) — never_apply node-hours; no applyable quickstart on terradart-validate
GoogleMemorystoreInstanceDesiredUserCreatedEndpoints: requires a parent GoogleMemorystoreInstance (never_apply); deferred with the instance

# Vertex AI indexes bill Vector Search Index Building when content is
# ingested (C7E2-9256-1C43 SKU 8724-DA51-DA95 $3/GiBy); deployed indexes
# add serving capacity hours. Prediction endpoints are cheap shells until
# DeployModel starts Online/Batch Prediction node-hours. Quickstarts that
# force apply_smoke_skip (never_apply) or that would bill on deploy are
# deferred — factories ship without an example.
GoogleVertexAiDeploymentResourcePool: gcp-cost Vertex AI C7E2-9256-1C43 Online/Batch Prediction Americas N1 Core 84D9-B3FA-0886 $0.036/h — never_apply dedicated min replicas; no applyable quickstart on terradart-validate
GoogleVertexAiEndpoint: gcp-cost Vertex AI C7E2-9256-1C43 list_skus keyword Endpoint → 0; Online/Batch Prediction Americas N1 Core 84D9-B3FA-0886 $0.036/h after DeployModel — shell is safe but a quickstart that deploys a model would bill; no apply-smoke quickstart
GoogleVertexAiFeaturestore: gcp-cost Vertex AI C7E2-9256-1C43 Feature Store online serving node SKU 74D1-6081-582A $0.94/h — never_apply; no applyable quickstart on terradart-validate
GoogleVertexAiFeaturestoreEntitytype: requires a parent GoogleVertexAiFeaturestore (never_apply); deferred with the Featurestore Wave
GoogleVertexAiFeaturestoreEntitytypeFeature: requires a parent GoogleVertexAiFeaturestoreEntitytype; deferred with the Featurestore Wave
GoogleVertexAiFeatureOnlineStore: gcp-cost Vertex AI C7E2-9256-1C43 Feature Store optimized online node us-central1 SKU 565E-845D-9FC2 $0.3/h; Bigtable online node us-east1 SKU 2EA3-C6D3-6BFF $1.06/h — never_apply sealed storage Wave; no applyable quickstart on terradart-validate
GoogleVertexAiFeatureOnlineStoreFeatureview: requires a parent GoogleVertexAiFeatureOnlineStore (never_apply); deferred with the Feature Online Store Wave
GoogleVertexAiRagEngineConfig: gcp-cost Vertex AI C7E2-9256-1C43 list_skus RagManaged/RAG Engine → 0; Scaled tier is production RagManagedDb — never_apply; no applyable quickstart on terradart-validate
GoogleVertexAiReasoningEngine: gcp-cost Vertex AI C7E2-9256-1C43 ReasoningEngine management fee CPU 8A55-0B95-B7DC + Memory 0B45-6103-6EC1 — never_apply; no applyable quickstart on terradart-validate
GoogleVertexAiEndpointWithModelGardenDeployment: gcp-cost Vertex AI C7E2-9256-1C43 Online/Batch Prediction Americas N1 Core 84D9-B3FA-0886 $0.036/h — never_apply Model Garden deploy; no applyable quickstart on terradart-validate
GoogleVertexAiIndex: gcp-cost Vertex AI C7E2-9256-1C43 Vector Search Index Building SKU 8724-DA51-DA95 $3/GiBy — never_apply; no applyable quickstart on terradart-validate
GoogleVertexAiIndexEndpointDeployedIndex: gcp-cost Vertex AI C7E2-9256-1C43 Vector Search Index Serving e2-standard-2 us-central1 SKU 722D-2FE3-D851 $0.0938084/h — never_apply serving hours; no applyable quickstart on terradart-validate
GoogleVertexAiIndexEndpoint: deferred with never_apply GoogleVertexAiIndex / GoogleVertexAiIndexEndpointDeployedIndex — endpoint shell is cheap but an index + deploy would bill building/serving; no apply-smoke quickstart

# Apigee orgs/instances bill Gateway Node Hours / environment usage while
# provisioned (1C2D-8C78-EC58 Gateway Node Hours SKU 0136-18C1-DD41
# $1.025/h). A quickstart would force apply_smoke_skip (never_apply) and
# block wave auto-merge — factories ship without an example.
GoogleApigeeOrganization: gcp-cost Apigee 1C2D-8C78-EC58 Gateway Node Hours SKU 0136-18C1-DD41 $1.025/h (Intermediate env 421B-D6C0-52A2 $2/h; Comprehensive 01C8-CFFA-106E $4.7/h) — never_apply; no applyable quickstart on terradart-validate
GoogleApigeeInstance: gcp-cost Apigee 1C2D-8C78-EC58 Gateway Node Hours SKU 0136-18C1-DD41 $1.025/h — never_apply runtime; deferred with the organization Wave
GoogleApigeeInstanceAttachment: requires a parent GoogleApigeeInstance (never_apply); deferred with the instance
GoogleApigeeEnvironment: gcp-cost Apigee 1C2D-8C78-EC58 Active Base Environment Usage Hours SKU C112-9373-5FC4 $0.50/h (Intermediate 421B-D6C0-52A2 $2/h; Comprehensive 01C8-CFFA-106E $4.70/h) — never_apply; deferred with the organization Wave
GoogleBeyondcorpAppGateway: gcp-cost Chrome Enterprise Premium F91A-404B-8D2E Monthly Users SKU E2D2-474B-B4EF $6/user.mo — never_apply; CEP entitlement absent on terradart-validate
GoogleBeyondcorpAppConnector: requires never_apply GoogleBeyondcorpAppGateway + CEP; deferred with the BeyondCorp Wave
GoogleBeyondcorpAppConnection: requires never_apply GoogleBeyondcorpAppGateway / GoogleBeyondcorpAppConnector + CEP; deferred with the BeyondCorp Wave
GoogleBeyondcorpSecurityGateway: gcp-cost Chrome Enterprise Premium F91A-404B-8D2E Monthly Users SKU E2D2-474B-B4EF $6/user.mo — never_apply; CEP entitlement absent on terradart-validate
GoogleBeyondcorpSecurityGatewayApplication: requires never_apply GoogleBeyondcorpSecurityGateway + CEP; deferred with the BeyondCorp Wave
GoogleApigeeEnvgroup: deferred with the never_apply Apigee org Wave (hostname group metadata; no apply-smoke quickstart)
GoogleApigeeEnvgroupAttachment: requires never_apply GoogleApigeeEnvironment / GoogleApigeeEnvgroup; deferred with the org Wave
GoogleApigeeEnvironmentAddonsConfig: requires never_apply GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeAddonsConfig: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeDnsZone: needs never_apply GoogleApigeeOrganization plus a peered VPC — no standalone-project apply path; deferred with the org Wave
GoogleApigeeEndpointAttachment: needs never_apply GoogleApigeeOrganization and a real Private Service Connect service attachment — no standalone-project apply path; deferred with the org Wave
GoogleApigeeNatAddress: needs never_apply GoogleApigeeInstance (Gateway Node Hours); deferred with the organization Wave
GoogleApigeeSpace: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeSyncAuthorization: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeTargetServer: requires never_apply GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeDeveloper: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeDeveloperApp: requires never_apply GoogleApigeeOrganization / GoogleApigeeDeveloper; deferred with the org Wave
GoogleApigeeApiProduct: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeAppGroup: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeEnvKeystore: requires never_apply GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeEnvReferences: requires never_apply GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeEnvironmentKeyvaluemaps: requires never_apply GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeEnvironmentKeyvaluemapsEntries: requires never_apply GoogleApigeeEnvironment / GoogleApigeeEnvironmentKeyvaluemaps; deferred with the org Wave
GoogleApigeeKeystoresAliasesSelfSignedCert: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment / GoogleApigeeEnvKeystore; deferred with the org Wave
GoogleApigeeApi: requires never_apply GoogleApigeeOrganization (+ local proxy config_bundle); deferred with the org Wave
GoogleApigeeApiDeployment: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeSharedflow: requires never_apply GoogleApigeeOrganization (+ local sharedflow config_bundle); deferred with the org Wave
GoogleApigeeSharedflowDeployment: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment / GoogleApigeeSharedflow; deferred with the org Wave
GoogleApigeeFlowhook: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment / GoogleApigeeSharedflow; deferred with the org Wave
GoogleApigeeEnvironmentDebugmask: requires never_apply GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeKeystoresAliasesKeyCertFile: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment / GoogleApigeeEnvKeystore; deferred with the org Wave
GoogleApigeeKeystoresAliasesPkcs12: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment / GoogleApigeeEnvKeystore; deferred with the org Wave
GoogleApigeeControlPlaneAccess: requires never_apply GoogleApigeeOrganization; deferred with the org Wave
GoogleApigeeEnvironmentApiRevisionDeployment: requires never_apply GoogleApigeeOrganization / GoogleApigeeEnvironment; deferred with the org Wave
GoogleApigeeSecurityFeedback: requires never_apply GoogleApigeeOrganization + Advanced API Security; deferred with the org Wave
GoogleApigeeSecurityProfileV2: requires never_apply GoogleApigeeOrganization + Advanced API Security; deferred with the org Wave
GoogleDiscoveryEngineLicenseConfig: gcp-cost Vertex AI Search 74B1-77CF-C302 Gemini Enterprise Standard monthly SKU 0532-C2F0-1DF0 $35/seat.mo — never_apply existence/seat subscription (License Manager class; MM exclude_delete); no applyable quickstart
GoogleDiscoveryEngineUserStore: gcp-cost Vertex AI Search 74B1-77CF-C302 Gemini Enterprise Standard monthly SKU 0532-C2F0-1DF0 $35/seat.mo — never_apply seat auto-register via defaultLicenseConfig (MM exclude_delete); no applyable quickstart
GoogleDiscoveryEngineWidgetConfig: gcp-cost Vertex AI Search 74B1-77CF-C302 Gemini Enterprise Standard monthly SKU 0532-C2F0-1DF0 $35/seat.mo — never_apply Agentspace widget (MM exclude_delete); no applyable quickstart
GoogleDiscoveryEngineAssistant: gcp-cost Vertex AI Search 74B1-77CF-C302 Gemini Enterprise Standard monthly SKU 0532-C2F0-1DF0 $35/seat.mo — never_apply Agentspace assistant / Gemini Enterprise entitlement; no applyable quickstart
GoogleDiscoveryEngineChatEngine: gcp-cost Vertex AI Search 74B1-77CF-C302 Gemini Enterprise Standard monthly SKU 0532-C2F0-1DF0 $35/seat.mo (+ Dialogflow CX session A1CC-751A-CDCC $0.20) — never_apply Agentspace chat engine; no applyable quickstart
GoogleDiscoveryEngineRecommendationEngine: gcp-cost Vertex AI Search 74B1-77CF-C302 Gemini Enterprise Standard monthly SKU 0532-C2F0-1DF0 $35/seat.mo — never_apply Agentspace recommendation engine; no applyable quickstart
GoogleSpannerInstancePartition: gcp-cost Cloud Spanner CC63-0873-48FD Enterprise RW Replica Iowa SKU 1D9F-E99D-5CBD $0.41/h (regional node Zurich 07AA-3267-AE40 $1.17/h) — never_apply reserved Spanner compute; no applyable quickstart
GoogleSpannerBackupSchedule: gcp-cost Cloud Spanner CC63-0873-48FD Backup storage Regional us-east1 SKU D026-0717-CF8E $0.1/GiBy.mo — never_apply retained backup bytes; no applyable quickstart
GoogleKmsEkmConnection: gcp-cost Cloud KMS EE2F-D110-890C Active external symmetric key versions SKU E0AA-8721-5338 $3/mo — never_apply (MM exclude_delete; external EKM); no applyable quickstart
GoogleDialogflowCxAgent: gcp-cost Cloud Dialogflow FBC0-AA4A-C89A Text session SKU A1CC-751A-CDCC $0.20/session (Audio 9496-0679-69BE $0.45) — never_apply CX agent runtime; no applyable quickstart
GoogleDialogflowCxSecuritySettings: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxGenerator: deferred with never_apply GoogleDialogflowCxAgent (CX generative path)
GoogleDialogflowCxTool: deferred with never_apply GoogleDialogflowCxAgent (CX tool path)
GoogleDialogflowCxFlow: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxIntent: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxPage: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxPlaybook: deferred with never_apply GoogleDialogflowCxAgent (CX generative session path)
GoogleDialogflowCxWebhook: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxGenerativeSettings: gcp-cost Cloud Dialogflow FBC0-AA4A-C89A Text session SKU A1CC-751A-CDCC $0.20/session — never_apply generative defaults (MM exclude_delete); deferred with GoogleDialogflowCxAgent
GoogleDialogflowCxEntityType: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxEnvironment: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleDialogflowCxVersion: deferred with never_apply GoogleDialogflowCxAgent (CX flow version path)
GoogleDialogflowCxToolVersion: deferred with never_apply GoogleDialogflowCxAgent (CX tool path)
GoogleDialogflowCxTestCase: deferred with never_apply GoogleDialogflowCxAgent (CX session path)
GoogleNetworkServicesGateway: gcp-cost Networking E505-1604-58F8 Cloud SWP Standard Gateway SKU 884B-E0E7-C3E4 $1.25/h — never_apply; no applyable quickstart on terradart-validate
GoogleNetworkServicesEdgeCacheService: gcp-cost Networking E505-1604-58F8 Media CDN Capacity Reservation NA SKU 7393-8C37-77E1 $20000/mo (+ Edge Cache egress E2B8-D4FA-6E05 $0.02/GiBy) — never_apply; no applyable quickstart on terradart-validate
GoogleNetworkServicesEdgeCacheOrigin: requires never_apply GoogleNetworkServicesEdgeCacheService; deferred with the Media CDN Edge Cache Wave
GoogleNetworkServicesEdgeCacheKeyset: requires never_apply GoogleNetworkServicesEdgeCacheService; deferred with the Media CDN Edge Cache Wave
GoogleGeminiCodeRepositoryIndex: gcp-cost Duet AI 719A-983F-202D Gemini Code Assist subscription SKU 7743-4D2E-8A79 $19/mo (Enterprise 78B4-81D7-89D8 $45/mo) — never_apply; seat/entitlement path; no applyable quickstart on terradart-validate
GoogleGeminiCodeToolsSetting: gcp-cost Duet AI 719A-983F-202D Gemini Code Assist subscription SKU 7743-4D2E-8A79 $19/mo (Enterprise 78B4-81D7-89D8 $45/mo) — never_apply; seat/entitlement path; no applyable quickstart on terradart-validate
GoogleGeminiCodeToolsSettingBinding: requires never_apply GoogleGeminiCodeToolsSetting (+ Code Assist subscription); deferred with the Code Assist tools Wave
GoogleSecureSourceManagerHook: requires never_apply GoogleSecureSourceManagerInstance (+ repository); deferred with the SSM instance Wave
GoogleSecureSourceManagerBranchRule: requires never_apply GoogleSecureSourceManagerInstance (+ repository); deferred with the SSM instance Wave
GoogleBlockchainNodeEngineBlockchainNodes: gcp-cost Blockchain Node Engine 1749-9ED8-C6DC Ethereum Full SKU 8884-3FAF-A138 $0.69/h — never_apply; no applyable quickstart on terradart-validate
GoogleCloudIdsEndpoint: gcp-cost Cloud IDS 25DB-618E-3F79 Endpoint Usage SKU FB57-C6D5-4F05 $1.5/h — never_apply; no applyable quickstart on terradart-validate
GoogleComputeStoragePool: gcp-cost Compute Engine 6F81-5844-456A Hyperdisk Balanced Storage Pools Standard Capacity Iowa SKU 5BC4-9775-DEC9 $0.08/GiBy.mo — never_apply; no applyable quickstart on terradart-validate
GoogleNetworkSecurityFirewallEndpoint: gcp-cost Network Security E749-01A2-AE1F Cloud NGFW Enterprise Endpoint Uptime SKU B778-1457-4A22 $1.75/h — never_apply; org-scoped parent; no applyable quickstart on terradart-validate
GoogleNetworkSecurityFirewallEndpointAssociation: requires never_apply GoogleNetworkSecurityFirewallEndpoint; deferred with the NGFW endpoint Wave
GoogleNetworkSecurityInterceptDeployment: gcp-cost Network Security E749-01A2-AE1F OOB Deployment Uptime SKU E55D-280B-82DD $0.025/h — never_apply; no applyable quickstart
GoogleNetworkSecurityInterceptDeploymentGroup: deferred with never_apply GoogleNetworkSecurityInterceptDeployment (OOB intercept path)
GoogleNetworkSecurityInterceptEndpointGroup: deferred with never_apply GoogleNetworkSecurityInterceptDeployment (OOB intercept path)
GoogleNetworkSecurityInterceptEndpointGroupAssociation: deferred with never_apply GoogleNetworkSecurityInterceptDeployment (OOB intercept path)
GoogleNetworkSecurityMirroringDeployment: gcp-cost Network Security E749-01A2-AE1F OOB Deployment Uptime SKU E55D-280B-82DD $0.025/h — never_apply; no applyable quickstart
GoogleNetworkSecurityMirroringDeploymentGroup: deferred with never_apply GoogleNetworkSecurityMirroringDeployment (OOB mirroring path)
GoogleNetworkSecurityTlsInspectionPolicy: gcp-cost Network Security E749-01A2-AE1F Cloud NGFW Enterprise Endpoint Uptime SKU B778-1457-4A22 $1.75/h (+ Data Processing 994B-C7B9-C1F7 $0.0193/GiBy) — never_apply; configures NGFW TLS decryption; no applyable quickstart on terradart-validate
GoogleNetworkSecuritySecurityProfile: gcp-cost Network Security E749-01A2-AE1F Cloud NGFW Enterprise Endpoint Uptime SKU B778-1457-4A22 $1.75/h — never_apply; configures NGFW / out-of-band inspection; no applyable quickstart on terradart-validate
GoogleNetworkSecuritySecurityProfileGroup: requires never_apply GoogleNetworkSecuritySecurityProfile (+ NGFW endpoint path); deferred with the NGFW security-profile Wave
GoogleChronicleEnvironment: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps instance; no applyable quickstart on terradart-validate
GoogleChronicleRule: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps instance; no applyable quickstart on terradart-validate
GoogleChronicleRuleDeployment: requires never_apply GoogleChronicleRule (+ entitlement-gated SecOps); deferred with the Chronicle rule Wave
GoogleChronicleRetrohunt: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; historical reprocessing on entitlement-gated SecOps; no applyable quickstart on terradart-validate
GoogleChronicleReferenceList: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps; no applyable quickstart on terradart-validate
GoogleChronicleWatchlist: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps; no applyable quickstart on terradart-validate
GoogleChronicleDataExport: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; historical export on entitlement-gated SecOps (+ GCS); no applyable quickstart on terradart-validate
GoogleChronicleParser: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; ingestion-path parser on entitlement-gated SecOps; no applyable quickstart on terradart-validate
GoogleChronicleFindingsRefinement: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps; no applyable quickstart on terradart-validate
GoogleChronicleFindingsRefinementDeployment: requires never_apply GoogleChronicleFindingsRefinement (+ entitlement-gated SecOps); deferred with the Chronicle findings-refinement Wave
GoogleChronicleDataAccessLabel: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps RBAC; no applyable quickstart on terradart-validate
GoogleChronicleDataAccessScope: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps RBAC; no applyable quickstart on terradart-validate
GoogleChronicleDataTable: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; entitlement-gated SecOps lookup tables; no applyable quickstart on terradart-validate
GoogleChronicleDataTableRow: requires never_apply GoogleChronicleDataTable (+ entitlement-gated SecOps); deferred with the Chronicle data-table Wave
GoogleChronicleParserExtension: gcp-cost Chronicle 144D-4907-2A21 Enterprise Plus US ingestion SKU 0310-AEE4-5DC1 $6.58/GBy — never_apply; ingestion-path extension on entitlement-gated SecOps; no applyable quickstart on terradart-validate
GoogleDataflowJob: gcp-cost Cloud Dataflow 57D6-8E6B-2DE0 vCPU Time Batch Iowa SKU A613-4169-2E08 $0.056/h per vCPU — never_apply; no applyable quickstart on terradart-validate
GoogleIntegrationConnectorsConnection: gcp-cost Integration Connectors 6FFB-B71E-5A0F Connection nodes Google managed apps SKU 4AB5-4E41-8DAB $0.35/h (business apps 4E3B-04D1-77FA $0.70/h) — never_apply; no applyable quickstart on terradart-validate
GoogleIntegrationConnectorsEndpointAttachment: requires never_apply Integration Connectors connection path (+ real PSC service attachment); deferred with the connectors Wave
GoogleIntegrationConnectorsManagedZone: requires never_apply Integration Connectors connection path (+ real target VPC); deferred with the connectors Wave
GoogleOracleDatabaseCloudExadataInfrastructureExascaleConfig: gcp-cost Bare Metal Solution 6955-34DC-6EF3 All Flash NVMe Storage Iowa SKU A094-33FD-1ECD $115/mo per TB — never_apply; requires Exadata infrastructure + Oracle entitlement

# Cloud Deploy IAM binding/policy — sibling *IamMember already in
# clouddeploy_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleClouddeployTargetIamBinding: iam-adjunct-debt: sibling GoogleClouddeployTargetIamMember already in clouddeploy_quickstart; authoritative binding not needed in smoke
GoogleClouddeployTargetIamPolicy: iam-adjunct-debt: sibling GoogleClouddeployTargetIamMember already in clouddeploy_quickstart; authoritative policy not needed in smoke
GoogleClouddeployDeliveryPipelineIamBinding: iam-adjunct-debt: sibling GoogleClouddeployDeliveryPipelineIamMember already in clouddeploy_quickstart; authoritative binding not needed in smoke
GoogleClouddeployDeliveryPipelineIamPolicy: iam-adjunct-debt: sibling GoogleClouddeployDeliveryPipelineIamMember already in clouddeploy_quickstart; authoritative policy not needed in smoke
GoogleClouddeployCustomTargetTypeIamBinding: iam-adjunct-debt: sibling GoogleClouddeployCustomTargetTypeIamMember already in clouddeploy_quickstart; authoritative binding not needed in smoke
GoogleClouddeployCustomTargetTypeIamPolicy: iam-adjunct-debt: sibling GoogleClouddeployCustomTargetTypeIamMember already in clouddeploy_quickstart; authoritative policy not needed in smoke

# Cloud Run v2 IAM binding/policy — sibling *IamMember already in
# cloud_run_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleCloudRunV2ServiceIamBinding: iam-adjunct-debt: sibling GoogleCloudRunV2ServiceIamMember already in cloud_run_quickstart; authoritative binding not needed in smoke
GoogleCloudRunV2ServiceIamPolicy: iam-adjunct-debt: sibling GoogleCloudRunV2ServiceIamMember already in cloud_run_quickstart; authoritative policy not needed in smoke
GoogleCloudRunV2JobIamBinding: iam-adjunct-debt: sibling GoogleCloudRunV2JobIamMember already in cloud_run_quickstart; authoritative binding not needed in smoke
GoogleCloudRunV2JobIamPolicy: iam-adjunct-debt: sibling GoogleCloudRunV2JobIamMember already in cloud_run_quickstart; authoritative policy not needed in smoke
GoogleCloudRunV2WorkerPoolIamBinding: iam-adjunct-debt: sibling GoogleCloudRunV2WorkerPoolIamMember already in cloud_run_quickstart; authoritative binding not needed in smoke
GoogleCloudRunV2WorkerPoolIamPolicy: iam-adjunct-debt: sibling GoogleCloudRunV2WorkerPoolIamMember already in cloud_run_quickstart; authoritative policy not needed in smoke

# Cloud KMS IAM binding/policy — sibling *IamMember already in
# kms_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleKmsKeyRingIamBinding: iam-adjunct-debt: sibling GoogleKmsKeyRingIamMember already in kms_quickstart; authoritative binding not needed in smoke
GoogleKmsKeyRingIamPolicy: iam-adjunct-debt: sibling GoogleKmsKeyRingIamMember already in kms_quickstart; authoritative policy not needed in smoke
GoogleKmsCryptoKeyIamBinding: iam-adjunct-debt: sibling GoogleKmsCryptoKeyIamMember already in kms_quickstart; authoritative binding not needed in smoke
GoogleKmsCryptoKeyIamPolicy: iam-adjunct-debt: sibling GoogleKmsCryptoKeyIamMember already in kms_quickstart; authoritative policy not needed in smoke

# IAP App Engine IAM binding/policy — sibling *IamMember already in
# iam_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleIapAppEngineServiceIamBinding: iam-adjunct-debt: sibling GoogleIapAppEngineServiceIamMember already in iam_quickstart; authoritative binding not needed in smoke
GoogleIapAppEngineServiceIamPolicy: iam-adjunct-debt: sibling GoogleIapAppEngineServiceIamMember already in iam_quickstart; authoritative policy not needed in smoke
GoogleIapAppEngineVersionIamBinding: iam-adjunct-debt: sibling GoogleIapAppEngineVersionIamMember already in iam_quickstart; authoritative binding not needed in smoke
GoogleIapAppEngineVersionIamPolicy: iam-adjunct-debt: sibling GoogleIapAppEngineVersionIamMember already in iam_quickstart; authoritative policy not needed in smoke
GoogleIapWebTypeAppEngineIamBinding: iam-adjunct-debt: sibling GoogleIapWebTypeAppEngineIamMember already in iam_quickstart; authoritative binding not needed in smoke
GoogleIapWebTypeAppEngineIamPolicy: iam-adjunct-debt: sibling GoogleIapWebTypeAppEngineIamMember already in iam_quickstart; authoritative policy not needed in smoke

# Compute Engine IAM binding/policy — sibling *IamMember already in
# compute_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleComputeDiskIamBinding: iam-adjunct-debt: sibling GoogleComputeDiskIamMember already in compute_quickstart; authoritative binding not needed in smoke
GoogleComputeDiskIamPolicy: iam-adjunct-debt: sibling GoogleComputeDiskIamMember already in compute_quickstart; authoritative policy not needed in smoke
GoogleComputeInstanceIamBinding: iam-adjunct-debt: sibling GoogleComputeInstanceIamMember already in compute_quickstart; authoritative binding not needed in smoke
GoogleComputeInstanceIamPolicy: iam-adjunct-debt: sibling GoogleComputeInstanceIamMember already in compute_quickstart; authoritative policy not needed in smoke
GoogleComputeSubnetworkIamBinding: iam-adjunct-debt: sibling GoogleComputeSubnetworkIamMember already in compute_quickstart; authoritative binding not needed in smoke
GoogleComputeSubnetworkIamPolicy: iam-adjunct-debt: sibling GoogleComputeSubnetworkIamMember already in compute_quickstart; authoritative policy not needed in smoke

# Bigtable IAM binding/policy — sibling *IamMember already in
# bigtable_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleBigtableInstanceIamBinding: iam-adjunct-debt: sibling GoogleBigtableInstanceIamMember already in bigtable_quickstart; authoritative binding not needed in smoke
GoogleBigtableInstanceIamPolicy: iam-adjunct-debt: sibling GoogleBigtableInstanceIamMember already in bigtable_quickstart; authoritative policy not needed in smoke
GoogleBigtableTableIamBinding: iam-adjunct-debt: sibling GoogleBigtableTableIamMember already in bigtable_quickstart; authoritative binding not needed in smoke
GoogleBigtableTableIamPolicy: iam-adjunct-debt: sibling GoogleBigtableTableIamMember already in bigtable_quickstart; authoritative policy not needed in smoke

# Cloud Healthcare IAM binding/policy — sibling *IamMember already in
# healthcare_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleHealthcareDatasetIamBinding: iam-adjunct-debt: sibling GoogleHealthcareDatasetIamMember already in healthcare_quickstart; authoritative binding not needed in smoke
GoogleHealthcareDatasetIamPolicy: iam-adjunct-debt: sibling GoogleHealthcareDatasetIamMember already in healthcare_quickstart; authoritative policy not needed in smoke
GoogleHealthcareFhirStoreIamBinding: iam-adjunct-debt: sibling GoogleHealthcareFhirStoreIamMember already in healthcare_quickstart; authoritative binding not needed in smoke
GoogleHealthcareFhirStoreIamPolicy: iam-adjunct-debt: sibling GoogleHealthcareFhirStoreIamMember already in healthcare_quickstart; authoritative policy not needed in smoke
GoogleHealthcareDicomStoreIamBinding: iam-adjunct-debt: sibling GoogleHealthcareDicomStoreIamMember already in healthcare_quickstart; authoritative binding not needed in smoke
GoogleHealthcareDicomStoreIamPolicy: iam-adjunct-debt: sibling GoogleHealthcareDicomStoreIamMember already in healthcare_quickstart; authoritative policy not needed in smoke
GoogleHealthcareConsentStoreIamBinding: iam-adjunct-debt: sibling GoogleHealthcareConsentStoreIamMember already in healthcare_quickstart; authoritative binding not needed in smoke
GoogleHealthcareConsentStoreIamPolicy: iam-adjunct-debt: sibling GoogleHealthcareConsentStoreIamMember already in healthcare_quickstart; authoritative policy not needed in smoke
GoogleHealthcareHl7V2StoreIamBinding: iam-adjunct-debt: sibling GoogleHealthcareHl7V2StoreIamMember already in healthcare_quickstart; authoritative binding not needed in smoke
GoogleHealthcareHl7V2StoreIamPolicy: iam-adjunct-debt: sibling GoogleHealthcareHl7V2StoreIamMember already in healthcare_quickstart; authoritative policy not needed in smoke


# Dataproc Metastore IAM binding/policy — sibling *IamMember already in
# dataproc_metastore_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleDataprocMetastoreServiceIamBinding: iam-adjunct-debt: sibling GoogleDataprocMetastoreServiceIamMember already in dataproc_metastore_quickstart; authoritative binding not needed in smoke
GoogleDataprocMetastoreServiceIamPolicy: iam-adjunct-debt: sibling GoogleDataprocMetastoreServiceIamMember already in dataproc_metastore_quickstart; authoritative policy not needed in smoke
GoogleDataprocMetastoreFederationIamBinding: iam-adjunct-debt: sibling GoogleDataprocMetastoreFederationIamMember already in dataproc_metastore_quickstart; authoritative binding not needed in smoke
GoogleDataprocMetastoreFederationIamPolicy: iam-adjunct-debt: sibling GoogleDataprocMetastoreFederationIamMember already in dataproc_metastore_quickstart; authoritative policy not needed in smoke

# BigQuery dataset/table/connection IAM binding/policy — sibling *IamMember
# already in bigquery_quickstart (iam-adjunct-debt path; AGENTS.md Wave shipping).
GoogleBigqueryDatasetIamBinding: iam-adjunct-debt: sibling GoogleBigqueryDatasetIamMember already in bigquery_quickstart; authoritative binding not needed in smoke
GoogleBigqueryDatasetIamPolicy: iam-adjunct-debt: sibling GoogleBigqueryDatasetIamMember already in bigquery_quickstart; authoritative policy not needed in smoke
GoogleBigqueryTableIamBinding: iam-adjunct-debt: sibling GoogleBigqueryTableIamMember already in bigquery_quickstart; authoritative binding not needed in smoke
GoogleBigqueryTableIamPolicy: iam-adjunct-debt: sibling GoogleBigqueryTableIamMember already in bigquery_quickstart; authoritative policy not needed in smoke
GoogleBigqueryConnectionIamBinding: iam-adjunct-debt: sibling GoogleBigqueryConnectionIamMember already in bigquery_quickstart; authoritative binding not needed in smoke
GoogleBigqueryConnectionIamPolicy: iam-adjunct-debt: sibling GoogleBigqueryConnectionIamMember already in bigquery_quickstart; authoritative policy not needed in smoke
