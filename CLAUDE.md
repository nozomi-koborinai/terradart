# Claude Code Guide

Use `AGENTS.md` as the shared project guide. Read `CONTEXT.md` for project vocabulary before design work.

Do not duplicate long-lived policy here. Keep Claude-specific behavior minimal so Cursor, Codex, cloud agents, and Claude Code stay aligned through `AGENTS.md`.
