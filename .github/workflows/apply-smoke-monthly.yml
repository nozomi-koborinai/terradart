name: Apply smoke full sweep

on:
  schedule:
    # Monthly backstop: 1st of the month, 02:00 UTC. The per-PR change-gate
    # (apply-smoke.yml) verifies changed examples; this full sweep is the
    # regression backstop for provider/API drift the per-PR gate can't see.
    # After a provider/factory bump, run it manually (workflow_dispatch) to
    # verify the whole example set under the new provider.
    - cron: "0 2 1 * *"
  workflow_dispatch:

concurrency:
  group: apply-smoke
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write
  issues: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  sweep:
    name: monthly apply sweep (terradart-validate)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dart-lang/setup-dart@v1
        with:
          sdk: stable
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.11.0"
      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
      - run: dart pub get
      - name: Apply-smoke all quickstarts
        id: sweep
        env:
          GCP_VALIDATE_PROJECT_ID: ${{ secrets.GCP_VALIDATE_PROJECT_ID }}
        run: |
          chmod +x tool/apply_smoke.sh
          set -o pipefail
          tool/apply_smoke.sh --all 2>&1 | tee /tmp/sweep.log
      - name: Open or update failure issue
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          summary="$(grep -E 'FAILED:' /tmp/sweep.log | tail -1 || echo 'see run log')"
          title="monthly apply-smoke failures"
          body="$(printf 'Monthly apply sweep failed on %s.\n\n%s\n\nRun: %s' \
            "$(date -u +%Y-%m-%d)" "$summary" "$RUN_URL")"
          existing="$(gh issue list --repo "$REPO" --state open \
            --search "$title in:title" --json number --jq '.[0].number')"
          if [[ -n "$existing" ]]; then
            gh issue comment "$existing" --repo "$REPO" --body "$body"
          else
            gh issue create --repo "$REPO" --title "$title" --body "$body"
          fi
