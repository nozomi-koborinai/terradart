/// terradart-mcp entry point.
///
/// The real implementation (MCP server startup + `--version`) lands in Task B7.
/// This stub keeps the `executables:` declaration in pubspec.yaml from dangling.
Future<void> main(List<String> args) async {
  throw UnimplementedError(
    'terradart-mcp entry point is implemented in Task B7.',
  );
}
