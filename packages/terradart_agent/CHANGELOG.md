# Changelog

## 0.12.0

- Initial release: `terradart-mcp` MCP server exposing the curated GCP factory
  catalog (list_resources / list_barrels / get_resource_schema / get_quickstart),
  distributed as a binary via Homebrew. (Finalized during v0.12.0 release prep.)
