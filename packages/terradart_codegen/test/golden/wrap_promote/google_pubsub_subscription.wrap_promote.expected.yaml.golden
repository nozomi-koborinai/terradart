
# === wrap-promote additions (do not edit inside this block; regenerated by wrap-promote) ===
# Generated on 2026-01-01. Rename placeholders, copy/integrate into the main yaml above, fill in encode() bodies, then delete this block.

# --- validValues: dead_letter_policy_kind ---
# TODO(wrap-promote): rename `GooglePubsubSubscriptionDeadLetterPolicyKind` to a domain-appropriate name.
prelude: |
  enum GooglePubsubSubscriptionDeadLetterPolicyKind {
    kept,
    expired,
    archived;

    String get terraformValue {
      switch (this) {
        case GooglePubsubSubscriptionDeadLetterPolicyKind.kept: return 'kept';
        case GooglePubsubSubscriptionDeadLetterPolicyKind.expired: return 'expired';
        case GooglePubsubSubscriptionDeadLetterPolicyKind.archived: return 'archived';
      }
    }
  }

# dartTypeOverrides entry (uncomment + integrate into the main yaml):
# dartTypeOverrides:
#   dead_letter_policy_kind: GooglePubsubSubscriptionDeadLetterPolicyKind

# === end wrap-promote additions ===
