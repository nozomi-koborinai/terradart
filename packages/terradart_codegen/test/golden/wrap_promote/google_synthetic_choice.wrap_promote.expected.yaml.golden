
# === wrap-promote additions (do not edit inside this block; regenerated by wrap-promote) ===
# Generated on 2026-01-01. Rename placeholders, copy/integrate into the main yaml above, fill in encode() bodies, then delete this block.

# --- exactly_one_of group: option_a, option_b ---
# TODO(wrap-promote): rename `GoogleSyntheticChoiceChoice` and its helper classes below to domain names.
prelude: |
  sealed class GoogleSyntheticChoiceChoice {
    const GoogleSyntheticChoiceChoice();
    String get blockKey;
    Map<String, Object?> encode();
  }

  final class OptionA extends GoogleSyntheticChoiceChoice {
    const OptionA({
      required this.aValue,
    });
    final TfArg<String> aValue;

    @override
    String get blockKey => 'option_a';

    @override
    Map<String, Object?> encode() {
      // TODO(wrap-promote): implement encode for OptionA.
      // Pattern: { if (optional != null) 'optional_key': optional!.toTfJson(), 'required_key': required.toTfJson(), ... }
      throw UnimplementedError('TODO(wrap-promote): implement encode for OptionA');
    }
  }

  final class OptionB extends GoogleSyntheticChoiceChoice {
    const OptionB({
      required this.bValue,
      this.bFlag,
    });
    final TfArg<String> bValue;
    final TfArg<bool>? bFlag;

    @override
    String get blockKey => 'option_b';

    @override
    Map<String, Object?> encode() {
      // TODO(wrap-promote): implement encode for OptionB.
      // Pattern: { if (optional != null) 'optional_key': optional!.toTfJson(), 'required_key': required.toTfJson(), ... }
      throw UnimplementedError('TODO(wrap-promote): implement encode for OptionB');
    }
  }

customSlots:
  choice:
    paramDeclaration: 'required GoogleSyntheticChoiceChoice choice'
    argMapEntry: "choice.blockKey: TfArg.literal(choice.encode()),"

# === end wrap-promote additions ===
