outputDir: quicksight
deriveClassDoc: true
deriveOutputGetters: true

# Nesting reaches depth 14 with thousands of blocks; typed helpers would emit
# a single file of tens of MB. Nested blocks stay map passthroughs until the
# shared-shape helpers land.
deriveNestedTypes: false
